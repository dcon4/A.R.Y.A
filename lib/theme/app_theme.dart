import 'package:flutter/material.dart';

class MyAppTheme {
  // Deep green for primary text
  static const Color mainFontColor = Color.fromRGBO(76, 175, 80, 1);

  // Medium green for first suggestion box
  static const Color firstSuggestionBoxColor = Color.fromRGBO(102, 187, 106, 1);

  // Light green for second suggestion box
  static const Color secondSuggestionBoxColor = Color.fromRGBO(129, 199, 132, 1);

  // Teal green for third suggestion box
  static const Color thirdSuggestionBoxColor = Color.fromRGBO(38, 166, 154, 1);

  // Pale green glow for assistant circle
  static const Color assistantCircleColor = Color.fromRGBO(165, 214, 167, 1);

  // Muted green border
  static const Color borderColor = Color.fromRGBO(76, 175, 80, 0.5);

  static const Color blackColor = Colors.black;
  static const Color whiteColor = Colors.white;
}
